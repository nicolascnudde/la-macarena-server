// This file is automatically generated by Keystone, do not modify it manually.
// Modify your Keystone config when you want to change this.

datasource postgresql {
  url      = env("DATABASE_URL")
  provider = "postgresql"
}

generator client {
  provider = "prisma-client-js"
  output   = "node_modules/.prisma/client"
}

model Activity {
  id            String    @id @default(uuid()) @postgresql.Uuid
  title         String    @unique @default("")
  description   String    @default("")
  image         Json?
  price         Int
  numberOfSlots Int
  category      Category? @relation("Activity_category", fields: [categoryId], references: [id])
  categoryId    String?   @map("category") @postgresql.Uuid
  multipleDays  Boolean   @default(false)
  date          DateTime?
  fromDate      DateTime?
  toDate        DateTime?

  @@index([categoryId])
}

model Category {
  id                     String     @id @default(uuid()) @postgresql.Uuid
  title                  String     @unique @default("")
  description            String     @default("")
  image                  Json?
  from_Activity_category Activity[] @relation("Activity_category")
}

model Member {
  id        String @id @default(uuid()) @postgresql.Uuid
  firstName String @default("")
  lastName  String @default("")
  image     Json?
  origin    String @default("")
}

model User {
  id       String @id @default(uuid()) @postgresql.Uuid
  name     String @default("")
  email    String @unique @default("")
  password String
}